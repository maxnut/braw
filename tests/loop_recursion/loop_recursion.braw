fn lr_factorial(n: int) -> int {
    if(n <= 1):
        return 1;
    return n * lr_factorial(n - 1);
}

fn lr_sum(n: int) -> int {
    let sum: int = 0;
    let i: int = 0;
    while(i < n) {
        sum = sum + i;
        i = i + 1;
    }
    return sum;
}

