fn add(a: int, b: int) -> int: return a + b;

define call_add add(2, 1);

define subtraction(a, b) #a - #b;

define multiplication(a, b) #a * #b;

fn macro() -> int {
    let a: int = 2;
    return $subtraction($multiplication(a, 2), $call_add);
}